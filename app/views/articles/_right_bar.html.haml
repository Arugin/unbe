.sidebar
  %h3.right-bar
    Области
  %ul.navigation
    %li
      = active_link_to main_app.articles_path, active: :exclusive do
        Все статьи
    - article_areas.each do |article_area|
      %li
        = active_link_to main_app.by_area_articles_path(article_area: article_area.title), active: /#{article_area.title}$/ do
          = t article_area.title
    %li
      = active_link_to 'Свалка', main_app.garbage_articles_path
