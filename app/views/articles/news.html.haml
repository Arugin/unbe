- if @articles.blank?
  %p= t("NO_NEWS")
- else
  -@articles.each do |news|
    = render :partial => 'article_short_block',:locals => { :article => news }

- content_for(:right_bar) do
  = render :partial => 'index_bar'