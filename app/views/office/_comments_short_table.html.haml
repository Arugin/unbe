%table.articles-short.table-striped
  - comments.each do |comment|
    %tr
      %td
        = link_to polymorphic_path(comment.commentable) do
          %span= comment.commentable.short_title
      %td
        = comment.short_content.html_safe