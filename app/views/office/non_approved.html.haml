.row-fluid
  .span5
    %h4=title 'Неподтвержденные статьи'
  .span7.middle
    .pull-right
      = render :partial => "common/search_control", :locals => { :current_action_path => non_approved_articles_path }

#articles.articles.ujs-pagination

- content_for(:right_bar) do
  = render :partial => 'right_bar'
