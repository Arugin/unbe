$("#cycles").updateHtml("#{escape_javascript(render :partial => 'cycles_block')}");
$(".list-header").listHeader();
$(".sticky-header").stickyHeader();
$(".tag-selected").tagsPopover({ tags_input: "#{escape_javascript(render partial: "common/tags_popover_content", locals:{model: Cycle})}" })