- if activity.trackable.present?
  опубликовал статью
  = link_to activity.trackable.correct_title, activity.trackable
  в цикле
  = link_to activity.trackable.cycle_correct_title, activity.trackable.cycle
- else
  опубликовал статью, но неведомые силы прогневались и удалили её